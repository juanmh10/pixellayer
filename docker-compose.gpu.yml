# Docker Compose override for NVIDIA GPU Passthrough
# Usage: docker compose -f docker-compose.yml -f docker-compose.gpu.yml up -d
services:
  pixellayer:
    environment:
      - PIXELLAYER_DEVICE=cuda
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
